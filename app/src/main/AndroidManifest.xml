<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.noefluxa.tpcursadamoviles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".Class4BoundService"
            android:enabled="true"
            android:exported="false"></service>
        <service
            android:name=".Class4IntentService"
            android:exported="false" />
        <service
            android:name=".Class4Service"
            android:enabled="true"
            android:exported="false" />

        <activity android:name=".Class2Numbers" />
        <activity android:name=".Class4" />
        <activity android:name=".Class3" />
        <activity android:name=".Class2" />
        <activity android:name=".Class1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".Class4$MyBroadcastReceiver" />
    </application>

</manifest>